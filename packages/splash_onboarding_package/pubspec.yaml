name: splash_onboarding_package
description: Reusable splash and onboarding flow with liquid glass UI.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  liquid_theme_package:
    git:
      url: https://github.com/mykyusuf/ready_flutter_packages.git
      ref: main
      path: packages/liquid_theme_package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
